# Modern Python Development Toolchain Template

This repository provides a template for setting up a modern Python development environment using `FastAPI`, `uv`, `ruff`, `ty`, `pytest` and `sonarqube` for code quality analysis.

